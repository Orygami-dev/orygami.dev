<div class="mesh">
  <div class="mesh__glow mesh__glow--1"></div>
  <div class="mesh__glow mesh__glow--2"></div>
</div>

<style>
  .mesh {
    width: 100%;
    height: 100%;
    position: relative;
    overflow: hidden;
    border-radius: 30% 70% 70% 30% / 30% 30% 70% 70%;
    background: linear-gradient(
      -45deg,
      #5FB3A2,
      #3F8F7D,
      #8fd4c6,
      #F4C95D,
      #5FB3A2,
      #3cc4ab
    );
    background-size: 400% 400%;
    animation:
      gradient-shift 10s ease infinite,
      morph 20s ease-in-out infinite;
  }

  @keyframes gradient-shift {
    0% { background-position: 0% 50%; }
    25% { background-position: 100% 0%; }
    50% { background-position: 100% 50%; }
    75% { background-position: 0% 100%; }
    100% { background-position: 0% 50%; }
  }

  @keyframes morph {
    0%, 100% { border-radius: 30% 70% 70% 30% / 30% 30% 70% 70%; }
    25% { border-radius: 58% 42% 45% 55% / 45% 60% 40% 55%; }
    50% { border-radius: 50% 50% 33% 67% / 55% 35% 65% 45%; }
    75% { border-radius: 40% 60% 60% 40% / 60% 45% 55% 40%; }
  }

  .mesh__glow {
    position: absolute;
    border-radius: 50%;
    filter: blur(60px);
    pointer-events: none;
  }

  .mesh__glow--1 {
    width: 50%;
    height: 50%;
    top: 10%;
    left: 15%;
    background: rgba(255, 255, 255, 0.35);
    animation: float-1 8s ease-in-out infinite;
  }

  .mesh__glow--2 {
    width: 40%;
    height: 40%;
    bottom: 10%;
    right: 10%;
    background: rgba(244, 201, 93, 0.25);
    animation: float-2 11s ease-in-out infinite;
  }

  @keyframes float-1 {
    0%, 100% { transform: translate(0, 0); }
    50% { transform: translate(20%, 15%); }
  }

  @keyframes float-2 {
    0%, 100% { transform: translate(0, 0); }
    50% { transform: translate(-15%, -20%); }
  }
</style>
