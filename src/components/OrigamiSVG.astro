---
// Grue origami classique (tsuru) - vue 3/4
---

<div class="origami-crane">
  <svg viewBox="0 0 300 250" xmlns="http://www.w3.org/2000/svg">
    <defs>
      <!-- Faces du papier -->
      <linearGradient id="paper-light" x1="0%" y1="0%" x2="100%" y2="100%">
        <stop offset="0%" stop-color="#FFFFFF"/>
        <stop offset="100%" stop-color="#F8F8F5"/>
      </linearGradient>
      <linearGradient id="paper-mid" x1="0%" y1="100%" x2="100%" y2="0%">
        <stop offset="0%" stop-color="#F0F0EB"/>
        <stop offset="100%" stop-color="#E8E8E3"/>
      </linearGradient>
      <linearGradient id="paper-shadow" x1="100%" y1="0%" x2="0%" y2="100%">
        <stop offset="0%" stop-color="#E0E0DB"/>
        <stop offset="100%" stop-color="#D0D0CB"/>
      </linearGradient>
      <linearGradient id="paper-dark" x1="0%" y1="0%" x2="100%" y2="100%">
        <stop offset="0%" stop-color="#C8C8C3"/>
        <stop offset="100%" stop-color="#B8B8B3"/>
      </linearGradient>
      <filter id="drop-shadow" x="-30%" y="-30%" width="160%" height="160%">
        <feDropShadow dx="4" dy="8" stdDeviation="8" flood-color="#2F3A3A" flood-opacity="0.2"/>
      </filter>
    </defs>

    <g class="crane" filter="url(#drop-shadow)">

      <!-- QUEUE (arrière) -->
      <polygon
        points="150,145 135,175 150,210 165,175"
        fill="url(#paper-shadow)"
        stroke="#CDCDC8"
        stroke-width="0.5"
        stroke-linejoin="round"
      />

      <!-- AILE GAUCHE (dessous, plus sombre) -->
      <polygon
        points="150,110 70,95 25,115 40,135 90,145 130,130"
        fill="url(#paper-shadow)"
        stroke="#D5D5D0"
        stroke-width="0.5"
        stroke-linejoin="round"
      />
      <!-- Pli aile gauche -->
      <line x1="150" y1="110" x2="55" y2="115" stroke="#C0C0BB" stroke-width="0.8"/>

      <!-- AILE DROITE (dessus, plus claire) -->
      <polygon
        points="150,110 230,95 275,115 260,135 210,145 170,130"
        fill="url(#paper-mid)"
        stroke="#E0E0DB"
        stroke-width="0.5"
        stroke-linejoin="round"
      />
      <!-- Pli aile droite -->
      <line x1="150" y1="110" x2="245" y2="115" stroke="#D8D8D3" stroke-width="0.8"/>

      <!-- CORPS - losange central (partie haute, éclairée) -->
      <polygon
        points="150,110 130,130 150,145 170,130"
        fill="url(#paper-light)"
        stroke="#E5E5E0"
        stroke-width="0.5"
        stroke-linejoin="round"
      />

      <!-- COU - s'incline vers l'avant/droite -->
      <polygon
        points="150,110 145,95 160,55 175,75 165,100"
        fill="url(#paper-mid)"
        stroke="#E0E0DB"
        stroke-width="0.5"
        stroke-linejoin="round"
      />
      <!-- Pli du cou -->
      <line x1="150" y1="110" x2="162" y2="65" stroke="#D5D5D0" stroke-width="0.6"/>

      <!-- TÊTE -->
      <polygon
        points="160,55 155,48 175,30 185,45 175,55"
        fill="url(#paper-light)"
        stroke="#E8E8E3"
        stroke-width="0.5"
        stroke-linejoin="round"
      />

      <!-- BEC -->
      <polygon
        points="175,30 172,38 195,22 178,38"
        fill="url(#paper-dark)"
        stroke="#B5B5B0"
        stroke-width="0.4"
        stroke-linejoin="round"
      />

    </g>
  </svg>
</div>

<style>
  .origami-crane {
    width: 100%;
    height: 100%;
    display: flex;
    align-items: center;
    justify-content: center;
  }

  .origami-crane svg {
    width: 100%;
    height: 100%;
    max-width: 500px;
    max-height: 420px;
  }

  .crane {
    transform-origin: center;
    animation: float 4s ease-in-out infinite;
  }

  @keyframes float {
    0%, 100% { transform: translateY(0); }
    50% { transform: translateY(-12px); }
  }
</style>
